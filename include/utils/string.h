#ifndef SYNTHAX_UTILS_STRING_H
#define SYNTHAX_UTILS_STRING_H

#include <stddef.h>

size_t strlen(const char *s);
int    strcmp(const char *a, const char *b);
char  *strcopy(char *dst, const char *src);

#endif
